package com.example.expensense.fragments

import android.os.Bundle
import android.widget.TextView
import androidx.appcompat.app.AppCompatActivity
import com.example.expensense.Global
import com.example.expensense.R
import org.eazegraph.lib.charts.PieChart


class WeeklyChart : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_weekly_chart)

        // Create the object of TextView and PieChart class
        //var tvR: TextView? = null, var tvPython: // Create the object of TextView and PieChart class
        //TextView? = null,
       // var tvCPP: // Create the object of TextView and PieChart class
       //         TextView? = null, var tvJava: // Create the object of TextView and PieChart class
      //  TextView? = null
      //  var pieChart: PieChart? = null
        var expenses : TextView
        var expense : TextView
        var expens: TextView
        var expen : TextView
        var pieChart: PieChart




// Set the percentage of language used
       // expenses.setText(Integer.toString(40));
       // expens.setText(Integer.toString(30));
        //expen.setText(Integer.toString(5));
        //expense.setText(Integer.toString(25));
    }



}